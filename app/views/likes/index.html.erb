<%# &. shortcut @user && @user.full_name
if user is nil don't execute anything after &.
safe navigation operator
%><h1> Questions liked by <%= @user&.full_name %></h1>

<ul>
<% @questions.each do |q| %>
  <%# link_to q.title, question_path(q) %>
  <!-- 👇 this sugar syntax for 👆 -->
  <li><%= link_to q.title, q %></li>
<% end %>
</ul>
